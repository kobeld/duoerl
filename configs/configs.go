package configs

var (
	AppHost  = "localhost:8888"
	HttpPort = ":8888"
	DBUrl    = "localhost"
	Database = "duoerl"
)

const (
	EMAIL_REGEXP = `(^([^@\s]+)@((?:[-A-z0-9]+\.)+[A-z]{2,})$)|^$`

	DATE_BIRTHDAY = "2006-01-02"
)
