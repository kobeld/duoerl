package configs

var (
	AppHost       = ""
	HttpPort      = ":8888"
	DBUrl         = "localhost"
	Database      = "duoerl_p"
	AssetsVersion = 0
)

const (
	EMAIL_REGEXP = `(^([^@\s]+)@((?:[-A-z0-9]+\.)+[A-z]{2,})$)|^$`

	DATE_BIRTHDAY = "2006-01-02"
)
